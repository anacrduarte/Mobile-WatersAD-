<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="WatersAD.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WatersAD"
    xmlns:local1="clr-namespace:WatersAD.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="WatersAD">
    <!--<TabBar>
        <ShellContent Title="Home" Icon="home" ContentTemplate="{DataTemplate local1:HomePage}" />
        <ShellContent Title="Preços" Icon="pricelist" ContentTemplate="{DataTemplate local1:ServicePriceListPage}" />
        <ShellContent Title="Pedidos" Icon="watertap" ContentTemplate="{DataTemplate local1:RequestWaterMeterPage}" />
        <ShellContent Title="Perfil" Icon="account" ContentTemplate="{DataTemplate local1:ProfilePage}" />
    </TabBar>-->


</Shell>
